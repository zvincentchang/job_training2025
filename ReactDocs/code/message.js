const message = () => {
    const name = "Jesse";
    const age = 40;
    return name + ' is ' + age + ' years old.';
};
const name = "Jesse"
const age = 40

export default message;
export { name, age }
